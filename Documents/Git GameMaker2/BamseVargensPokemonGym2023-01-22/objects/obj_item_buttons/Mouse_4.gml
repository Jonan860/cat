/// @description Insert description here
// You can write your code in this editor

if(struct.appliable()){
struct.apply()
with(global.amber.active_pokemon){action_bar+=max_action_bar}
scr_turn_decision()
with(obj_item_buttons){instance_destroy()}
}
